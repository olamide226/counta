<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Dev builds get a distinct launcher name so they are easy to tell apart
         from the published app when both are installed. -->
    <application
        android:label="Counta Dev"
        tools:replace="android:label" />
</manifest>
